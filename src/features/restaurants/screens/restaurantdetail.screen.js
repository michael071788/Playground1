import React, { useContext, useState } from "react";
import { SafeAreaComponent } from "../../../components/utility/safearea.component";
import { Text, View } from "react-native";

export const RestaurantDetailScreen = ({ navigation }) => {
  return (
    <SafeAreaComponent>
      <View>
        <Text>RestaurantDetailScreensssss</Text>
      </View>
    </SafeAreaComponent>
  );
};
